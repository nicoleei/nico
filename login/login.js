window.onload=function(){
  $().getId("login").center(200,200).resize(function(){
    $().getId("login").center(200,200);
  })
 
//   window.onresize = function() {
//    var tempTop = (document.documentElement.clientHeight - 200) / 2;
//     var tempWidth = (document.documentElement.clientWidth - 200) / 2;
//     $().getId("login").css("top", tempTop + "px").css('left', tempWidth + 'px');

//   }
}